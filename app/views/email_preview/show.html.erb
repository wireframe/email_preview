<!DOCTYPE html>
<html>
<head>
  <title>Email Preview</title>
</head>
<frameset rows="250px,*">
  <frame src="<%= details_email_preview_path(params[:id]) %>" name="details" />
  <frame name="preview" src="<%= preview_email_preview_path(params[:id], :format => render_format(@parts.first)) %>" />
</frameset>
</html>
